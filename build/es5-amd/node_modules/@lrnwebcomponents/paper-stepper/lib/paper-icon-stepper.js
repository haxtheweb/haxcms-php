define(["../../../@polymer/polymer/polymer-legacy.js"],function(_polymerLegacy){"use strict";(0,_polymerLegacy.Polymer)({is:"paper-icon-stepper",properties:{/**
     * Indeces of the steps already selected
     */selectedValues:{type:Array,notify:!0/* transition */ /* transition */,value:[]},/**
     * Currently selected step
     */_selected:{type:Number,value:-1}},/**
   * Goes back one step
   */previous:function previous(){//XXX: Multi selector does not offer selectPrevious() function
if(0>this._selected){// we are back to start
return}// remove the currently selected from the array
var index=this.selectedValues.indexOf(this._selected);this.splice("selectedValues",index,1);this._selected--},/**
   * Goes forward one step
   */next:function next(){//XXX: Multi selector does not offer selectNext() function
if(this._selected===this._items.length-1){// We reached the end
return}if(0<=this._selected){this._selected++}else if(0>=this.selectedValues.length){// no items selected yet
this._selected=0}else{// items selected (e.g. specified by the caller). Default to the element after the last element of the array
this._selected=this.selectedValues[this.selectedValues.length]++}this.push("selectedValues",this._selected)},/**
   * Clears all the steps
   */clear:function clear(){this.selectedValues=[];this._selected=-1},_onItemsChanged:function _onItemsChanged(e){this._items=this.$.selector.items}})});