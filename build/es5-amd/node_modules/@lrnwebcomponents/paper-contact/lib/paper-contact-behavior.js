define(["../../../@polymer/polymer/lib/legacy/polymer.dom.js"],function(_polymerDom){"use strict";/**
 * Contact elements can use this behavior to open a detail view on tap. The user needs to implement
 * a method _getTargetUrl() which returns the URL that is opened.
 *
 * @polymerBehavior window.PaperContactBehavior
 */window.PaperContactBehavior={/**
   * Opens the target URL in the same tab
   */openInSameTab:function openInSameTab(e){var value=this._getContent();if(value){window.location.assign(encodeURI(this._getTargetUrl(value)))}},/**
   * Opens the target URL in a new tab
   */openInBlankTab:function openInBlankTab(e){var value=this._getContent();if(value){window.open(encodeURI(this._getTargetUrl(value),"_blank"))}},// Private methods
_getContent:function _getContent(e){// Content is stored as the text content of the element, e.g. <paper-contact-email>XXX</paper-contact-email>
var items=(0,_polymerDom.dom)(this).getEffectiveChildNodes();if(0>=items.length){return null}return(0,_polymerDom.dom)(items[0]).textContent}}});