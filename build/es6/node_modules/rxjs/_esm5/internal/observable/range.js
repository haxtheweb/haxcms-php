import{Observable}from"../Observable.js";export function range(start,count,scheduler){if(void 0===start){start=0}if(void 0===count){count=0}return new Observable(function(subscriber){var index=0,current=start;if(scheduler){return scheduler.schedule(dispatch,0,{index:index,count:count,start:start,subscriber:subscriber})}else{do{if(index++>=count){subscriber.complete();break}subscriber.next(current++);if(subscriber.closed){break}}while(!0)}})}export function dispatch(state){var start=state.start,index=state.index,count=state.count,subscriber=state.subscriber;if(index>=count){subscriber.complete();return}subscriber.next(start);if(subscriber.closed){return}state.index=index+1;state.start=start+1;this.schedule(state)}