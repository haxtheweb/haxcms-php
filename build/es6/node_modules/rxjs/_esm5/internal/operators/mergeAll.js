import{mergeMap}from"./mergeMap.js";import{identity}from"../util/identity.js";export function mergeAll(concurrent){if(void 0===concurrent){concurrent=Number.POSITIVE_INFINITY}return mergeMap(identity,concurrent)}