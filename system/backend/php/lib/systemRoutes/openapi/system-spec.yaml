openapi: 3.0.3
info:
  title: HAXcms System API
  description: Control-plane API for authentication, lifecycle/admin,
    discovery/meta, and system settings/configuration.
  version: 1.0.0
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
servers:
  - url: /
    description: System API base path
tags:
  - name: discovery
    description: System API discovery and OpenAPI document endpoints
  - name: session
    description: Authentication, token, and dashboard session bootstrap endpoints
  - name: lifecycle
    description: Site lifecycle management operations
  - name: settings
    description: System configuration and integration endpoints
  - name: actions
    description: Utility action endpoints for file and content conversion
paths:
  /system/api/v1:
    get:
      tags:
        - discovery
      operationId: systemApiDiscovery
      summary: Discover top-level system API links and capabilities
      security: []
      responses:
        "200":
          description: Discovery metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/openapi:
    get:
      tags:
        - discovery
      operationId: systemOpenapi
      summary: Return the system OpenAPI document (JSON default, YAML supported)
      security: []
      responses:
        "200":
          description: OpenAPI document
          content:
            application/json:
              schema:
                type: object
            application/yaml:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/openapi.json:
    get:
      tags:
        - discovery
      operationId: systemOpenapiJson
      summary: Return the system OpenAPI document as JSON
      security: []
      responses:
        "200":
          description: OpenAPI JSON document
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/openapi.yaml:
    get:
      tags:
        - discovery
      operationId: systemOpenapiYaml
      summary: Return the system OpenAPI document as YAML
      security: []
      responses:
        "200":
          description: OpenAPI YAML document
          content:
            application/yaml:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/session/login:
    post:
      tags:
        - session
      operationId: sessionLogin
      summary: Authenticate a user and establish a dashboard session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /system/api/v1/session:
    get:
      tags:
        - session
      operationId: sessionAuthGet
      summary: Validate a JWT and return authenticated session details
      description: >
        Anonymous login probe. Returns session details when a valid JWT is
        supplied and a minimal unauthenticated response otherwise. No bearer
        auth is required to reach this endpoint.
      security: []
      responses:
        "200":
          description: Authenticated session details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - session
      operationId: sessionAuthPost
      summary: Validate a JWT and return authenticated session details
      description: >
        Anonymous login probe. Returns session details when a valid JWT is
        supplied (body or query) and a minimal unauthenticated response
        otherwise. No bearer auth is required to reach this endpoint.
      security: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                jwt:
                  type: string
      responses:
        "200":
          description: Authenticated session details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/session/logout:
    post:
      tags:
        - session
      operationId: sessionLogout
      summary: Log out the active dashboard session
      security: []
      responses:
        "200":
          description: Logout response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/session/refresh:
    get:
      tags:
        - session
      operationId: sessionRefreshGet
      summary: Refresh dashboard access token
      security: []
      responses:
        "200":
          description: Refresh response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags:
        - session
      operationId: sessionRefreshPost
      summary: Refresh dashboard access token
      security: []
      responses:
        "200":
          description: Refresh response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /system/api/v1/session/user:
    get:
      tags:
        - session
      operationId: sessionUserGet
      summary: Return active user profile and dashboard preference data
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Active user profile data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - session
      operationId: sessionUserPost
      summary: Return active user profile and dashboard preference data
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Active user profile data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/session/connection-settings:
    get:
      tags:
        - session
      operationId: sessionConnectionSettingsGet
      summary: Return frontend connection settings bootstrap payload
      security: []
      responses:
        "200":
          description: Frontend connection settings payload
          content:
            application/javascript:
              schema:
                type: string
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags:
        - session
      operationId: sessionConnectionSettingsPost
      summary: Return frontend connection settings bootstrap payload
      security: []
      responses:
        "200":
          description: Frontend connection settings payload
          content:
            application/javascript:
              schema:
                type: string
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/session/connection-test:
    get:
      tags:
        - session
      operationId: sessionConnectionTestGet
      summary: Validate current auth state for dashboard bootstrap
      security: []
      responses:
        "200":
          description: Session validity state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags:
        - session
      operationId: sessionConnectionTestPost
      summary: Validate current auth state for dashboard bootstrap
      security: []
      responses:
        "200":
          description: Session validity state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
  /system/api/v1/sites:
    get:
      tags:
        - lifecycle
      operationId: listSites
      summary: List all sites available to the authenticated user
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Site listing payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - lifecycle
      operationId: createSite
      summary: Create a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Site creation response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/sites/{siteName}:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    get:
      tags:
        - lifecycle
      operationId: siteInfoGet
      summary: Return metadata for a specific site
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Site metadata response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lifecycle
      operationId: siteInfoPost
      summary: Return metadata for a specific site
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Site metadata response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /system/api/v1/sites/{siteName}/clone:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    post:
      tags:
        - lifecycle
      operationId: cloneSite
      summary: Clone an existing site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Site clone response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/sites/{siteName}/archive:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    post:
      tags:
        - lifecycle
      operationId: archiveSite
      summary: Archive a site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Site archive response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/sites/{siteName}/download:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    post:
      tags:
        - lifecycle
      operationId: downloadSite
      summary: Generate and return a downloadable site archive reference
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Site download response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/sites/{siteName}/download-skeleton:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    post:
      tags:
        - lifecycle
      operationId: downloadSiteSkeleton
      summary: Generate and return a reusable skeleton for a site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Site skeleton response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/sites/{siteName}/save-as-template:
    parameters:
      - $ref: "#/components/parameters/SiteName"
    post:
      tags:
        - lifecycle
      operationId: saveSiteAsTemplate
      summary: Save a site as a reusable system template
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteActionRequest"
      responses:
        "200":
          description: Save-as-template response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/integrations/app-store:
    get:
      tags:
        - settings
      operationId: generateAppStore
      summary: Generate app-store provider manifest for the editor
      parameters:
        - name: siteName
          in: query
          required: true
          schema:
            type: string
          description: Site machine name used to validate site token scope
      security:
        - siteTokenHeader: []
      responses:
        "200":
          description: App-store payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/integrations/app-store/providers/{provider}/search:
    get:
      tags:
        - settings
      operationId: searchAppStoreProvider
      summary: Broker a provider search through app-store integrations
      description: >
        Proxies a search request to a registered app-store provider (e.g.
        product-card, media, etc.) and returns the provider's response payload.
        Moved to the system API in D38; documented here per D57. Requires a
        valid bearer JWT and site token so only authenticated site contexts
        can broker upstream provider searches.
      parameters:
        - $ref: "#/components/parameters/AppStoreProvider"
        - name: siteName
          in: query
          required: true
          schema:
            type: string
          description: Site machine name used to validate site token scope
      security:
        - bearerAuth: []
          siteTokenHeader: []
      responses:
        "200":
          description: Provider search response payload
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    additionalProperties: true
                  - type: array
                    items:
                      type: object
                      additionalProperties: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/haxiamAddUserAccess:
    post:
      tags:
        - settings
      operationId: haxiamAddUserAccess
      summary: Grant a user access to a site (HAXiam managed deployments)
      description: >
        PHP-only route exposed by HAXiam-managed deployments to add user access
        to a site. Documented here per D57 for cross-platform spec parity;
        NodeJS deployments that do not run HAXiam will return 404.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                site:
                  type: string
                  description: Site machine name to grant access to
                user:
                  type: string
                  description: User name to grant access for
              additionalProperties: true
      responses:
        "200":
          description: User access granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/status:
    get:
      tags:
        - settings
      operationId: systemStatusGet
      summary: Return system status and environment diagnostics
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System status data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemStatusPost
      summary: Return system status and environment diagnostics
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System status data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/system/version:
    get:
      tags:
        - settings
      operationId: systemVersionGet
      summary: Return installed system version details
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System version details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemVersionPost
      summary: Return installed system version details
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System version details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/entities:
    get:
      tags:
        - settings
      operationId: systemEntitiesGet
      summary: Return system entity descriptors and links
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System entity descriptors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemEntitiesPost
      summary: Return system entity descriptors and links
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System entity descriptors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/schemas:
    get:
      tags:
        - settings
      operationId: systemSchemasGet
      summary: Return system schema descriptors and links
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System schema descriptors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemSchemasPost
      summary: Return system schema descriptors and links
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: System schema descriptors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/configuration/api-keys:
    get:
      tags:
        - settings
      operationId: getApiKeys
      summary: Return configured API keys and provider statuses
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: API key settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: getApiKeysPost
      summary: Return configured API keys and provider statuses (read alias of GET)
      description: >
        Read-only alias of GET /configuration/api-keys. POST is accepted for
        callers that prefer POST over GET but does not write; use PATCH to
        update API key settings. Single-user deployment assumption: the
        NodeJS backend does not model an admin/superUser tier; these settings
        are written by the single authenticated dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: API key settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - settings
      operationId: saveApiKeysPatch
      summary: Update API key settings
      description: >
        Write operation — updates API key settings. Single-user deployment
        assumption: the NodeJS backend does not model an admin/superUser
        tier; settings are written by the single authenticated dashboard
        user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiKeysSettings"
      responses:
        "200":
          description: API key update response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/configuration/media:
    get:
      tags:
        - settings
      operationId: getMediaSettings
      summary: Return media and upload configuration
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Media settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: getMediaSettingsPost
      summary: Return media and upload configuration (read alias of GET)
      description: >
        Read-only alias of GET /configuration/media. POST is accepted for
        callers that prefer POST over GET but does not write; use PATCH to
        update media settings. Single-user deployment assumption: the
        NodeJS backend does not model an admin/superUser tier; these settings
        are written by the single authenticated dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Media settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - settings
      operationId: saveMediaSettingsPatch
      summary: Update media and upload configuration
      description: >
        Write operation — updates media and upload configuration. Single-user
        deployment assumption: the NodeJS backend does not model an
        admin/superUser tier; settings are written by the single authenticated
        dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MediaSettings"
      responses:
        "200":
          description: Media settings update response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/configuration/schema-files/operations:
    post:
      tags:
        - settings
      operationId: schemaFileOperation
      summary: Perform schema file operation in system configuration storage
      description: >
        Write operation — performs schema file operations (rename, delete,
        upload) in system configuration storage. Single-user deployment
        assumption: the NodeJS backend does not model an admin/superUser
        tier; operations are performed by the single authenticated dashboard
        user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Schema file operation response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/blocks:
    get:
      tags:
        - settings
      operationId: systemBlocksGet
      summary: Return available system blocks
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available block list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemBlocksPost
      summary: Return available system blocks (read alias of GET)
      description: >
        Read-only alias of GET /blocks. POST is accepted for callers that
        prefer POST over GET but does not write; use PATCH to update enabled
        block configuration. Single-user deployment assumption: the NodeJS
        backend does not model an admin/superUser tier; these settings are
        written by the single authenticated dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available block list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - settings
      operationId: saveEnabledBlocksPatch
      summary: Update enabled block configuration using the blocks collection endpoint
      description: >
        Write operation — updates enabled block configuration. Single-user
        deployment assumption: the NodeJS backend does not model an
        admin/superUser tier; settings are written by the single authenticated
        dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnabledCollectionSettings"
      responses:
        "200":
          description: Enabled collection update response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/skeletons:
    get:
      tags:
        - settings
      operationId: systemSkeletonsGet
      summary: Return available system skeletons
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available skeleton list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemSkeletonsPost
      summary: Return available system skeletons (read alias of GET)
      description: >
        Read-only alias of GET /skeletons. POST is accepted for callers that
        prefer POST over GET but does not write; use PATCH to update enabled
        skeleton configuration. Single-user deployment assumption: the NodeJS
        backend does not model an admin/superUser tier; these settings are
        written by the single authenticated dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available skeleton list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - settings
      operationId: saveEnabledSkeletonsPatch
      summary: Update enabled skeleton configuration using the skeletons collection
        endpoint
      description: >
        Write operation — updates enabled skeleton configuration. Single-user
        deployment assumption: the NodeJS backend does not model an
        admin/superUser tier; settings are written by the single authenticated
        dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnabledCollectionSettings"
      responses:
        "200":
          description: Enabled collection update response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/skeletons/{skeletonName}:
    parameters:
      - $ref: "#/components/parameters/SkeletonName"
    get:
      tags:
        - settings
      operationId: systemSkeletonDetailGet
      summary: Return details for a named skeleton
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Skeleton details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - settings
      operationId: systemSkeletonDetailPatch
      summary: Rename a named skeleton
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkeletonRenameRequest"
      responses:
        "200":
          description: Skeleton rename response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Target skeleton name already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    put:
      tags:
        - settings
      operationId: systemSkeletonDetailPut
      summary: Rename a named skeleton (compatibility alias for PATCH)
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkeletonRenameRequest"
      responses:
        "200":
          description: Skeleton rename response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Target skeleton name already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    delete:
      tags:
        - settings
      operationId: systemSkeletonDetailDelete
      summary: Delete a named skeleton
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Skeleton delete response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /system/api/v1/themes:
    get:
      tags:
        - settings
      operationId: systemThemesGet
      summary: Return available system themes
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available theme list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - settings
      operationId: systemThemesPost
      summary: Return available system themes (read alias of GET)
      description: >
        Read-only alias of GET /themes. POST is accepted for callers that
        prefer POST over GET but does not write; use PATCH to update enabled
        theme configuration. Single-user deployment assumption: the NodeJS
        backend does not model an admin/superUser tier; these settings are
        written by the single authenticated dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      responses:
        "200":
          description: Available theme list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - settings
      operationId: saveEnabledThemesPatch
      summary: Update enabled theme configuration using the themes collection endpoint
      description: >
        Write operation — updates enabled theme configuration. Single-user
        deployment assumption: the NodeJS backend does not model an
        admin/superUser tier; settings are written by the single authenticated
        dashboard user.
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnabledCollectionSettings"
      responses:
        "200":
          description: Enabled collection update response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/docx-to-html:
    post:
      tags:
        - actions
      operationId: docxToHtml
      summary: Convert an uploaded .docx or .doc file to clean HTML
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document (.docx or .doc) to convert
      responses:
        "200":
          description: Converted HTML content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/import-docx:
    post:
      tags:
        - actions
      operationId: importDocx
      summary: Convert an uploaded .docx or .doc file into a HAXcms site schema
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document (.docx or .doc) to convert
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Site schema items from the converted document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/import-pptx:
    post:
      tags:
        - actions
      operationId: importPptx
      summary: Convert an uploaded .pptx file into a HAXcms site schema
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PowerPoint file (.pptx) to convert
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Site schema items from the converted presentation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/import-html:
    post:
      tags:
        - actions
      operationId: importHtml
      summary: Convert an uploaded HTML file into a HAXcms site schema
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: HTML file (.html or .htm) to convert
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Site schema items from the converted HTML
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/import-xlsx:
    post:
      tags:
        - actions
      operationId: importXlsx
      summary: Convert an uploaded Excel file into a HAXcms site schema
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel file (.xlsx or .xls) to convert
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Site schema items from the converted Excel file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/import-pdf:
    post:
      tags:
        - actions
      operationId: importPdf
      summary: Convert an uploaded PDF file into a HAXcms site schema
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file (.pdf) to convert
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Site schema items from the converted PDF
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/html-to-docx:
    post:
      tags:
        - actions
      operationId: htmlToDocx
      summary: Convert HTML string to a DOCX document (returned as base64)
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                html:
                  type: string
                  description: HTML string to convert to DOCX
      responses:
        "200":
          description: Base64-encoded DOCX document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/md-to-html:
    post:
      tags:
        - actions
      operationId: mdToHtml
      summary: Convert a Markdown string to HTML
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                md:
                  type: string
                  description: Markdown string to convert
                type:
                  type: string
                  description: If set to 'link', the md value is treated as a URL to fetch
      responses:
        "200":
          description: Converted HTML content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/html-to-md:
    post:
      tags:
        - actions
      operationId: htmlToMd
      summary: Convert an HTML string to Markdown
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                html:
                  type: string
                  description: HTML string to convert
                type:
                  type: string
                  description: If set to 'link', the html value is treated as a URL to fetch
      responses:
        "200":
          description: Converted Markdown content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/pretty-html:
    post:
      tags:
        - actions
      operationId: prettyHtml
      summary: Pretty-print an HTML string
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                html:
                  type: string
                  description: HTML string to format
                type:
                  type: string
                  description: If set to 'link', the html value is treated as a URL to fetch
      responses:
        "200":
          description: Formatted HTML content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/json-to-yaml:
    post:
      tags:
        - actions
      operationId: jsonToYaml
      summary: Convert JSON to YAML
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                json:
                  description: JSON object or string to convert
                type:
                  type: string
                  description: If set to 'link', the json value is treated as a URL to fetch
      responses:
        "200":
          description: Converted YAML content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/yaml-to-json:
    post:
      tags:
        - actions
      operationId: yamlToJson
      summary: Convert YAML to JSON
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                yaml:
                  type: string
                  description: YAML string to convert
                type:
                  type: string
                  description: If set to 'link', the yaml value is treated as a URL to fetch
      responses:
        "200":
          description: Converted JSON content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/html-to-pdf:
    post:
      tags:
        - actions
      operationId: htmlToPdf
      summary: Convert HTML string to a PDF document (returned as base64)
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                html:
                  type: string
                  description: HTML string to convert to PDF
                base:
                  type: string
                  description: Optional base URL for resolving relative links
      responses:
        "200":
          description: Base64-encoded PDF document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/xlsx-to-csv:
    post:
      tags:
        - actions
      operationId: xlsxToCsv
      summary: Convert an uploaded Excel file to CSV
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel file (.xlsx) to convert
      responses:
        "200":
          description: CSV content and sheet metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/pdf-to-html:
    post:
      tags:
        - actions
      operationId: pdfToHtml
      summary: Convert an uploaded PDF file to semantic HTML
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        "200":
          description: Converted semantic HTML content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/pptx-to-html:
    post:
      tags:
        - actions
      operationId: pptxToHtml
      summary: Convert an uploaded PPTX file to HTML
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PowerPoint file (.pptx) to convert
      responses:
        "200":
          description: Converted HTML content and extracted files
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/actions/docx-to-pdf:
    post:
      tags:
        - actions
      operationId: docxToPdf
      summary: Convert an uploaded DOCX file to a PDF document
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document (.docx) to convert to PDF
      responses:
        "200":
          description: PDF document binary download
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
  /system/api/v1/site/import/haxcms:
    post:
      tags:
        - actions
      operationId: haxcmsToSite
      summary: Import a HAXcms site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/html:
    post:
      tags:
        - actions
      operationId: htmlToSite
      summary: Import an HTML page or site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Uploaded HTML file for import
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/pressbooks:
    post:
      tags:
        - actions
      operationId: pressbooksToSite
      summary: Import a Pressbooks site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/gitbook:
    post:
      tags:
        - actions
      operationId: gitbookToSite
      summary: Import a GitBook site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/notion:
    post:
      tags:
        - actions
      operationId: notionToSite
      summary: Import a Notion site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/wordpress:
    post:
      tags:
        - actions
      operationId: wordpressToSite
      summary: Import a WordPress site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/elmsln:
    post:
      tags:
        - actions
      operationId: elmslnToSite
      summary: Import an ELMSLN site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/drupal-book:
    post:
      tags:
        - actions
      operationId: drupalBookToSite
      summary: Import a Drupal Book site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/plone:
    post:
      tags:
        - actions
      operationId: ploneToSite
      summary: Import a Plone site as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/recipe:
    post:
      tags:
        - actions
      operationId: recipeToSite
      summary: Import a recipe as a new site
      security:
        - bearerAuth: []
          userTokenHeader: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                recipe:
                  type: object
                  description: Inline recipe object (alternative to repoUrl or file upload)
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Uploaded recipe file for import
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /system/api/v1/site/import/{platform}:
    post:
      tags:
        - actions
      operationId: siteImportByPlatform
      summary: Import a site from a supported platform by platform name
      security:
        - bearerAuth: []
          userTokenHeader: []
      parameters:
        - name: platform
          in: path
          required: true
          schema:
            type: string
          description: Platform identifier (haxcms, html, pressbooks, gitbook, notion, wordpress, elmsln, drupal-book, plone, recipe)
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Uploaded file for import (supported by some platforms)
                repoUrl:
                  type: string
                  description: URL to fetch for remote platform import
                method:
                  type: string
                  description: Import method (site, branch, or page)
                type:
                  type: string
                  description: Fallback content type (course, portfolio, or empty)
                parentId:
                  type: string
                  description: Optional parent ID for imported items
      responses:
        "200":
          description: Imported site schema items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unprocessable entity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
components:
  parameters:
    SiteName:
      name: siteName
      in: path
      required: true
      schema:
        type: string
      description: Machine name of the site
    SkeletonName:
      name: skeletonName
      in: path
      required: true
      schema:
        type: string
      description: Machine name of the skeleton
    AppStoreProvider:
      name: provider
      in: path
      required: true
      schema:
        type: string
      description: Registered app-store provider machine name
  responses:
    BadRequest:
      description: Malformed or invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unauthorized:
      description: Authentication is required
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Forbidden:
      description: Authenticated but not permitted to perform this operation
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFound:
      description: Requested resource was not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    userTokenHeader:
      type: apiKey
      in: header
      name: X-HAXCMS-User-Token
    siteTokenHeader:
      type: apiKey
      in: header
      name: X-HAXCMS-Site-Token
  schemas:
    ApiEnvelope:
      type: object
      properties:
        status:
          type: integer
        data: {}
      additionalProperties: true
    ErrorEnvelope:
      type: object
      required:
        - status
        - data
      properties:
        status:
          type: integer
        data:
          type: object
          additionalProperties: true
          properties:
            message:
              type: string
    LoginRequest:
      type: object
      description: >
        Login credentials or JWT revalidation token. When username/password are
        supplied the server validates them and issues a fresh JWT. When jwt is
        supplied (body or query) the server revalidates the existing token and
        returns it if still valid, enabling a revalidate-without-credentials
        login flow.
      properties:
        username:
          type: string
          description: Username (primary field name accepted by v1 handlers)
        password:
          type: string
          description: Password
        u:
          type: string
          description: Legacy username alias (v0 compatibility)
        p:
          type: string
          description: Legacy password alias (v0 compatibility)
        jwt:
          type: string
          description: >
            Existing JWT to revalidate. When supplied (body or query) the
            server validates the token and returns it if still valid.
      additionalProperties: true
    SiteActionRequest:
      type: object
      properties:
        site:
          type: object
      additionalProperties: true
    ApiKeysSettings:
      type: object
      description: API key settings payload
      additionalProperties: true
    MediaSettings:
      type: object
      description: Media settings payload
      additionalProperties: true
    EnabledCollectionSettings:
      type: object
      description: Enabled item map payload
      additionalProperties: true
    SkeletonUploadRequest:
      type: object
      description: Multipart payload for uploading a new skeleton resource
      properties:
        file:
          type: string
          format: binary
          description: Skeleton JSON file upload
        name:
          type: string
          description: Optional target skeleton machine name override
        schema:
          type: string
          description: Optional schema selector (defaults to skeleton)
        action:
          type: string
          description: Optional action hint (upload)
      additionalProperties: true
    SkeletonMutationRequest:
      type: object
      description: JSON mutation payload for skeleton resource operations
      properties:
        schema:
          type: string
        action:
          type: string
        name:
          type: string
        oldName:
          type: string
        skeletonName:
          type: string
        newName:
          type: string
        displayName:
          type: string
      additionalProperties: true
    SkeletonRenameRequest:
      type: object
      description: Rename payload for skeleton resource detail mutation
      required:
        - newName
      properties:
        newName:
          type: string
        displayName:
          type: string
      additionalProperties: true
